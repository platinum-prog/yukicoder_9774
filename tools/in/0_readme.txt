入力生成プログラムの使い方

実行ファイル(0_generator.out)が既に生成されています。
このディレクトリに移動して以下のようにコマンドを入力することでサンプルを生成できます。

./0_generator.out 100 0

一番目の引数は生成するテストケースの数です。
二番目の引数はテストケースの最初の番号です。
上の例では、シード値が0〜99のサンプルが生成されます。

入力生成プログラムを改変したい場合は同梱の0_generator.cppを編集しコンパイルしてください。